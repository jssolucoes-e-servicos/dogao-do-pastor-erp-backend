//src/common/configs/payment.config.ts

/**
 * Configurações de meios de pagamento
 * @returns {type} Retorna as configurações de meios de pagamento
 */
export const paymentConfig = {
  // Altere este valor para 'stripe' ou outro provedor conforme necessário.
  // Isso define qual provedor será usado no sistema.
  provider: 'mercadopago',
};
