name: Deploy to Production

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger EasyPanel deploy
        run: curl -X GET "${{ secrets.EASYPANEL_DEPLOY_URL }}"
